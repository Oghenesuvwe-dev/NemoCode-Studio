// Centralized icon exports with Phosphor Icons (Modern, Professional)
// Using different weights for different contexts:
// - Regular (default): Main UI elements
// - Light: Sidebar, secondary elements
// - Bold: Emphasis, active states
// - Fill: Selected/active icons

// Regular weight icons (default for most UI)
export {
    File as FileText,
    MagnifyingGlass as Search,
    Gear as Settings,
    Play,
    Pause,
    Stop as Square,
    Activity,
    Terminal,
    Cpu,
    ChatCircle as MessageSquare,
    CaretDown as ChevronDown,
    DotsThree as MoreHorizontal,
    Users,
    Command,
    ArrowClockwise as RefreshCw,
    Minus,
    X,
    Rocket,
    FolderOpen,
    SpeakerHigh as Volume2,
    SpeakerSlash as VolumeX,
    Microphone as Mic,
    PaperPlaneTilt as Send,
    ArrowsOut as Maximize,
    Square as SquareIcon,
    CircleNotch as Loader,
} from 'phosphor-react';

// Light weight icons (for subtle UI elements)
export {
    File as FileLight,
    MagnifyingGlass as SearchLight,
    Gear as SettingsLight,
} from 'phosphor-react';

// Bold weight icons (for emphasis)
export {
    CheckCircle,
    XCircle,
    WarningCircle as AlertCircle,
    Info,
} from 'phosphor-react';
